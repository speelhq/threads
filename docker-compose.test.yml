services:
  test-db:
    image: postgres:18
    environment:
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
      POSTGRES_DB: threads_test
    ports:
      - "5433:5432"
    tmpfs:
      - /var/lib/postgresql
